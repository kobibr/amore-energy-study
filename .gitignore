
*~
/[0-9]*.sh
1.sh
*.bak
*.bak.day*
*.before-*
build/
# Build / cache
# Calibration logs — explicit per-file commits only
check_status.sh
# Debug scripts (one-off bash scripts named numerically)
dist/
.DS_Store
# Editor / IDE settings — local only
*.egg-info/
internal/
!internal/.gitkeep
logs/
measurement/calibration-logs/*.csv
measurement/traces/
!measurement/traces/.gitkeep
.mypy_cache/
# OS junk
*.out
pkk_moc.log
# Private working notes
*.pyc
__pycache__/
*.py[cod]
.pytest_cache/
# Python virtualenvs (you use conda sage_env; harmless to ignore stray ones)
review_bundle.txt
review_bundle_*.zip
.ruff_cache/
# Scratch and tooling
status_report.txt
*.swp
/tmp/
# Trace data — mock/PPK2 writes here, never committed
.venv/
venv/
.vscode/
# Workspace backups (e.g. .before-O3-doc) — never committed

# mini_regression auto-saves PNGs to /figures/ — not for the paper
# (the paper uses report/figures/ which is committed)
/figures/

# pdflatex build artifacts (we DO commit main.pdf as the deliverable)
report/main.aux
report/main.log
report/main.out
report/main.toc
report/*.synctex.gz

# main.tex backups (transient, kept in git history)
report/main.tex.before_*
report/main.tex.bak


# Local tooling
commit_and_sanity.sh
do_everything.sh
analysis/dedekind_discovery_final_v46.md

# Day 5 additions
*.diff
*.pre-energy-backup
*.pre-*-backup
*.bak.*
amore.sh
output.txt
status.sh
# scratch debug reports
*_report.txt

# garbage calibration runs (uncalibrated PPK2, no reference resistor)
measurement/calibration-logs/calibration_2026060*_R33.txt
report/figures/
measurement/full-runs/
measurement/overnight-20260528/
docs/methodology.md
