
*~
/[0-9]*.sh
1.sh
*.bak
*.bak.day*
*.before-*
build/
# Build / cache
# Calibration logs — explicit per-file commits only
check_status.sh
# Debug scripts (one-off bash scripts named numerically)
dist/
.DS_Store
# Editor / IDE settings — local only
*.egg-info/
internal/
!internal/.gitkeep
logs/
measurement/calibration-logs/*.csv
measurement/traces/
!measurement/traces/.gitkeep
.mypy_cache/
# OS junk
*.out
pkk_moc.log
# Private working notes
*.pyc
__pycache__/
*.py[cod]
.pytest_cache/
# Python virtualenvs (you use conda sage_env; harmless to ignore stray ones)
review_bundle.txt
review_bundle_*.zip
.ruff_cache/
# Scratch and tooling
status_report.txt
*.swp
/tmp/
# Trace data — mock/PPK2 writes here, never committed
.venv/
venv/
.vscode/
# Workspace backups (e.g. .before-O3-doc) — never committed
